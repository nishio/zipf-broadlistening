# PLAN

## 目的 (MVP)
- 初見ユーザーに「ランダムサンプリングだと 1% の意見を落としやすい」を体験で伝える。
- 最初の版はパラメータを絞る (`alpha` は固定)。

## 固定シナリオ
- 総数 `N = 10000`
- 意見 `X` の件数 `n_X = 100` (1%)
- 調査リソース `K = 25`
- ランダムサンプリング試行回数 `T = 100`
- Zipf の `alpha` は固定値 (UI で変更しない)

## 体験フロー
1. 「母集団 10000件のうち、X は 100件」を表示。
2. ランダム調査を `T=100` 回実行 (各回 `K=25` 件を重複なし抽出)。
3. 各回で `X` が1件以上観測されたかを判定。
4. 観測あり/なし件数と観測確率を表示。
5. 同時に、意見分布を件数降順で棒グラフ上位 25 件表示し、`X` を強調表示。

## 実装要件
- `HTML + CSS + JS` の静的ページで完結。
- 乱数で毎回結果は揺れるが、理論値 `1 - exp(-K * n_X / N)` と並べて比較表示する。
- 分布グラフは「上位25クラスタ」を表示し、`X` は色/ラベルで明確に識別。
- `alpha` はコード定数として固定 (将来の拡張ポイントとしてコメントだけ残す)。

## データ・文言の分離 (多言語対応準備)
- 画面の説明文・ラベル・注記は JSON から読む。
- 想定ファイル:
  - `content/ja.json` (初期)
  - 将来 `content/en.json` などを追加
- 最低限の JSON キー例:
  - `title`
  - `story_intro`
  - `labels.population`
  - `labels.sample_size`
  - `labels.trials`
  - `labels.observed`
  - `labels.missed`
  - `labels.observed_rate`
  - `labels.theoretical_rate`
  - `chart.top_clusters`
  - `chart.highlight_x`

## 受け入れ条件
- 1ページを開くだけでシナリオが再生される。
- `T=100` 回結果が表示され、`X` 観測確率が確認できる。
- 棒グラフ上位25件に `X` の位置が示される。
- 文言はハードコードせず JSON 由来になっている。
- `alpha` 調整 UI は出さない (固定)。

## 次フェーズ (MVP後)
- [x] URL ハッシュパラメータでのシナリオ変更 (`#N=&K=&ni=&T=&a=&M=&g=`)。README に一覧あり。
- [x] パラメータ変更 UI (フォーム) を追加 (`playground.html`)。
- [x] ランディング/解説と実験室の2ページ構成に分割。`index.html` = ドット可視化ヒーロー + 解説、`playground.html` = パラメータ実験室。文言はそれぞれ `content/ja_landing.json` / `content/ja.json`。
- [ ] 言語切替 (`ja/en/tw`) のドロップダウン追加。
